from invert.utils import genome_utils

def extract_ranges(chrom, trans_ranges):
	seqs = []


def create(genome_file, out_dir, chrom_id, trans_ranges):
	chrom = genome_utils.get_chromosome(genome_file, chrom_id=chrom_id)
	
	
